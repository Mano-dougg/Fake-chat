class mensagerService {

    static async responseText(text: string) {
        // lógica da inteligência artificial

        const response = "resposta da inteligência artificial";

        return response;
    }
}

export default mensagerService;